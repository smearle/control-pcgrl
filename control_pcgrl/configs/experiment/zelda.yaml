# @package _global_
hydra:
  sweeper:
    params:
      # model: seqnca
      model: default_model, seqnca
      # task: binary_big, binary_bigger, zelda, zelda_big, zelda_bigger
      # task: binary_big, zelda, zelda_big
      task: zelda
      exp_id: 0, 1, 2
  
  # launcher:
  #   mem_gb: 70

name: zelda
