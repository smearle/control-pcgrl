# @package _global_
hydra:
  sweeper:
    params:
      # multiagent.n_agents: 1, 2, 3, 4
      representation: turtle

      # Aha, this should work to address proble below, right?
      multiagent: shared_policy

      multiagent.n_agents: 2, 3
      show_agents: True, False

